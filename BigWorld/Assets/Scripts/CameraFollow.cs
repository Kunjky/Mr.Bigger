using UnityEngine;
using System.Collections;

public class CameraFollow : MonoBehaviour {
	/*
	public float xRange; 
	public Transform target;
	public Rigidbody targetBody;
	private Transform m_transform;
	private bool following;
	private Vector3 des;
	void Start () {
		following=false;
		m_transform=this.transform;
	}

	// Update is called once per frame
	void Update () {
		Vector3 des=new Vector3(target.transform.position.x+Input.GetAxis("Horizontal")*-0.2f,m_transform.position.y,m_transform.position.z);
		m_transform.position=des;
	}

*/
}
